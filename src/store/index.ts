import { createStore } from "vuex";

export default createStore({
  state: {
    userInfo: {
      name: "lester",
      corpName: "上帝是个女孩",
    },
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
